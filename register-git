#!/bin/bash

if [ $# -gt 1 ]; then
    echo "Just two arguments are allowed."
    exit 1
fi

if [ "$1" -eq 0 ]; then
    git config user.name "Aayush Karki"
    git config user.email "aayush.ecobees@gmail.com"
else
    echo 'Could not register the user or wrong argument.'
fi